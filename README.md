# Tabby

Tabby is a lightweight task manager for todos, deadlines, and events. It provides a command-line interface and a responsive JavaFX GUI.

## Quick start

Run the GUI from the project directory:

```bash
./gradlew run
```

Use `help` inside Tabby to see the available commands. Tasks are saved automatically in `data/tabby.txt`.

For complete setup instructions, features, command formats, date formats, and error-handling guidance, see the [Tabby User Guide](docs/README.md).
